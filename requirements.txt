# Production dependencies for baidu-download system
# Core dependencies
paramiko>=2.12.0
pymysql>=1.1.1
python-dotenv>=1.0.1
colorama==0.4.6
requests>=2.28.0

# SFTP client dependency (pysftp is a wrapper around paramiko)
pysftp>=0.2.9

# Security dependencies (locked for Python 3.8 compatibility)
cryptography==41.0.7  # Required for Python 3.8 compatibility

# WeChat article PDF generation dependencies
playwright>=1.40.0  # PDF generation (only solution)
beautifulsoup4>=4.9.0  # HTML parsing for article metadata extraction
